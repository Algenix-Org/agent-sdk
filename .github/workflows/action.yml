name: 'Action'
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Default Step
        run: echo "Add your steps here"
        shell: bash